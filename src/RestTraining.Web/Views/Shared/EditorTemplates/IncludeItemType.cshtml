@using RestTraining.Web.Helper
@using RestTraining.Common.DTO

@model IncludeItemTypeDTO

@{
    Layout = null;
    var isDisabled = ViewData["isDisabled"] != null;
}

@Html.EnumDropDownListFor(model => model, Model, null, isDisabled)
    
@Html.ValidationMessageFor(model => model)